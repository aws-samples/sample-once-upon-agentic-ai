[project]
name = "once-upon-agentic-ai"
version = "1.0.0"
description = "A comprehensive workshop for mastering AI agent development with Strands framework"
requires-python = ">=3.10"
dependencies = [
    # Strands AI Framework — floor pinned because chapter 5 relies on the
    # A2AServer(agent_factory=...) API introduced in 1.44.
    "strands-agents[a2a]>=1.44",
    "strands-agents-tools",

    # MCP: pin to 1.x. Chapter 4 uses "from mcp.server import FastMCP", which was
    # renamed to MCPServer in mcp 2.x, so unpinned installs break the chapter.
    "mcp>=1.0,<2",

    # Web Framework for APIs
    "fastapi[standard]",
    "pydantic",
    "uvicorn",

    # Configuration Management
    "python-dotenv",

    # Knowledge Base & Document Processing
    "chromadb",
    "PyPDF2",

    # Database
    "tinydb",

    # LangChain Integration
    "langchain_aws",
    "langchain_chroma",

    # Ollama Integration
    "ollama",
]
